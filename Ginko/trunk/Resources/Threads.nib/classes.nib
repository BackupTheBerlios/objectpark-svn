{
    IBClasses = (
        {
            CLASS = DNDArrayController; 
            LANGUAGE = ObjC; 
            OUTLETS = {dndDelegate = id; }; 
            SUPERCLASS = NSArrayController; 
        }, 
        {
            ACTIONS = {selectTreeCell = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = GICommentTreeView; LANGUAGE = ObjC; SUPERCLASS = NSMatrix; }, 
        {CLASS = GIMatrixWithKeyboardSupport; LANGUAGE = ObjC; SUPERCLASS = NSMatrix; }, 
        {CLASS = GITextView; LANGUAGE = ObjC; SUPERCLASS = NSTextView; }, 
        {
            ACTIONS = {search = id; }; 
            CLASS = GIThreadsController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                commentTreeView = GICommentTreeView; 
                infoSplitView = NSSplitView; 
                messageDateColumn = NSTableColumn; 
                messageTextScrollView = NSScrollView; 
                messageTextView = NSTextView; 
                messagesController = NSArrayController; 
                searchField = NSSearchField; 
                subjectAndAuthorColumn = NSTableColumn; 
                threadDateColumn = NSTableColumn; 
                "thread_messageSplitView" = NSSplitView; 
                threadsController = NSArrayController; 
                threadsTableView = NSTableView; 
                verticalSplitView = NSSplitView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = GIThreadsWindow; LANGUAGE = ObjC; SUPERCLASS = NSWindow; }
    ); 
    IBVersion = 1; 
}