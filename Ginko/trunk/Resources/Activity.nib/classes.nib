{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {stopJob = id; }; 
            CLASS = GIActivityPanelController; 
            LANGUAGE = ObjC; 
            OUTLETS = {tableView = NSTableView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}