{
    IBClasses = (
        {
            ACTIONS = {
                addBcc = id; 
                addCc = id; 
                addFolder = id; 
                addReplyTo = id; 
                applySortingAndFiltering = id; 
                changeQuoteLevel = id; 
                emtpyTrashMailbox = id; 
                exportGroup = id; 
                extractThread = id; 
                followup = id; 
                forward = id; 
                getNewMailInAllAccounts = id; 
                importMboxFile = id; 
                joinThreads = id; 
                makeFontBigger = id; 
                makeFontSmaller = id; 
                moveSelectionToTrash = id; 
                newMessage = id; 
                pasteAsQuotation = id; 
                rename = id; 
                replyAll = id; 
                replyDefault = id; 
                replySender = id; 
                saveMessage = id; 
                selectThreadsWithCurrentSubject = id; 
                sendQueuedMessages = id; 
                showActivityPanel = id; 
                showGroupInspector = id; 
                showPhraseBrowser = id; 
                showRawSource = id; 
                toggleDisplay = id; 
                toggleReadFlag = id; 
                toggleRich = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                openNewGroupWindow = id; 
                openSearchWindow = id; 
                saveAction = id; 
                showActivityPanel = id; 
                toggleAutomaticActivityPanel = id; 
            }; 
            CLASS = GIApplication; 
            LANGUAGE = ObjC; 
            OUTLETS = {searchController = GISearchController; }; 
            SUPERCLASS = NSApplication; 
        }, 
        {
            ACTIONS = {toggleDisplay = id; }; 
            CLASS = GroupController; 
            LANGUAGE = ObjC; 
            OUTLETS = {messageBodyView = NSTextView; tabView = NSTabView; tableView = NSTableView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {openPreferences = id; }; 
            CLASS = NSApplication; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSResponder; 
        }
    ); 
    IBVersion = 1; 
}