{
    IBClasses = (
        {
            ACTIONS = {
                addBcc = id; 
                addCc = id; 
                addFolder = id; 
                addMessageGroup = id; 
                addReplyTo = id; 
                applySortingAndFiltering = id; 
                changeQuoteLevel = id; 
                dontChangeStandardApp = id; 
                emptyTrashMailbox = id; 
                exportGroup = id; 
                extractThread = id; 
                followup = id; 
                forward = id; 
                fulltextIndexSomeMessages = id; 
                getNewMailInAllAccounts = id; 
                importMboxFile = id; 
                joinThreads = id; 
                makeFontBigger = id; 
                makeFontSmaller = id; 
                makeGinkoStandardApp = id; 
                moveSelectionToTrash = id; 
                newMessage = id; 
                pasteAsQuotation = id; 
                rename = id; 
                repairInReplyTo = id; 
                replyAll = id; 
                replyDefault = id; 
                replySender = id; 
                resetFulltextIndex = id; 
                saveMessage = id; 
                selectThreadsWithCurrentSubject = id; 
                sendAndReceiveInAllAccounts = id; 
                sendQueuedMessages = id; 
                showActivityPanel = id; 
                showGroupInspector = id; 
                showPhraseBrowser = id; 
                showRawSource = id; 
                toggleDisplay = id; 
                toggleJunkFlag = id; 
                toggleReadFlag = id; 
                toggleRich = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                dontChangeStandardApp = id; 
                makeGinkoStandardApp = id; 
                saveAction = id; 
                sendMessagesDueInNearFuture = id; 
                showActivityPanel = id; 
                startFulltextIndexingJobIfNeeded = id; 
                toggleAutomaticActivityPanel = id; 
            }; 
            CLASS = GIApplication; 
            LANGUAGE = ObjC; 
            OUTLETS = {standardAppWindow = NSWindow; }; 
            SUPERCLASS = NSApplication; 
        }, 
        {
            ACTIONS = {toggleDisplay = id; }; 
            CLASS = GroupController; 
            LANGUAGE = ObjC; 
            OUTLETS = {messageBodyView = NSTextView; tabView = NSTabView; tableView = NSTableView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {openPreferences = id; }; 
            CLASS = NSApplication; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSResponder; 
        }
    ); 
    IBVersion = 1; 
}