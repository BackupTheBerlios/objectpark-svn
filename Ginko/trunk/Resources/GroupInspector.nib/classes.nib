{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {switchProfile = id; switchType = id; }; 
            CLASS = GIGroupInspectorController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                profileButton = NSPopUpButton; 
                typeRadioButtons = NSMatrix; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}