{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {switchProfile = id; }; 
            CLASS = G3GroupInspectorController; 
            LANGUAGE = ObjC; 
            OUTLETS = {profileButton = NSPopUpButton; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}